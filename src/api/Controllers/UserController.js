class UserController {
  /**
   * @param {import('express').Request} req
   * @param {import('express').Response} res
   */
  static sendSignupOtpController = (req, res) => {
    
  };
}

module.exports = UserController;
